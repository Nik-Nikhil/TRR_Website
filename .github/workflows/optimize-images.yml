name: Optimize Old Images

# Disabled — image compression happens client-side on upload via ImageOptimizationService
# The original script used browser APIs (canvas, FileReader) incompatible with Node.js
on:
  workflow_dispatch: # Manual trigger only, effectively disabled

jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Image optimization is handled client-side on upload. No server-side job needed."
